@model AspNetCoreApp.Web.Models.MailModel

@{
    Layout=null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width"/>
    <title>Index</title>
</head>
<body>
    <div>
        @using (Html.BeginForm("Index","SendMailer",FormMethod.Post,new{enctype="multipart/form-data"}))
        {
        <center>
            <hr />
            <table>
                <tr>
                    <td style="width:80px">
                        To:
                    </td>
                    <td>@Html.TextBoxFor(model=>model.To)</td>
                </tr>
                <tr>
                    <td>
                        Subject:
                    </td>
                    <td>@Html.TextBoxFor(model=>model.Subject)</td>
                </tr>
                <tr>
                    <td valign:"top">
                        Body:
                    </td>
                    <td>@Html.TextAreaFor(model=>model.Body,new {rows="15",cols="25"})</td>
                </tr>
                <tr>
                    <td>
                        <input type="submit" value="Send" />
                    </td>
                </tr>
            </table>
            <span style="color:green">@ViewBag.Message</span>
        </center>


        @*@Html.ValidationSummary()
        <p>To: </p>
        <p>@Html.TextBoxFor(m=>m.To)</p>
        <p>Subject: </p>
        <p>@Html.TextBoxFor(m=>m.Subject)</p>
        <p>Body: </p>
        <p>@Html.TextAreaFor(m=>m.Body)</p>
        <input type="submit" value="Send" />*@
   }

    </div>
</body>
</html>

